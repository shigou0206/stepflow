[tool.poetry]
name = "stepflow"
version = "0.1.0"
description = "StepFlow for statemachine"
authors = ["Zhihao Liu <liuzhihao0206@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.13"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
